# 项目需求文档 (PRD)：文件快递柜 (FileExpress)

## 1. 项目概述
本项目是一个轻量级、极简风格的临时文件存储与共享平台。用户可以通过取件码或公开链接安全地传递临时文档。

**当前状态**：项目已进入改善阶段，已引入基础设施韧性增强方案。

## 2. 核心功能

### 2.1 基础交互
*   **极简 UI**：采用简约设计语言，支持**暗色 (Dark)** 和 **亮色 (Light)** 模式切换。
*   **交互逻辑**：主页采用“存放/提取”切换模式。布局紧凑，首屏可见广场内容。
*   **多语言支持**：支持**中文**和**英文**。
*   **使用协议/声明**：界面提供微型声明按钮。

### 2.2 公开广场 (Public Square)
*   **格式**：仅限 `.txt`。支持在线编辑标题和内容直接生成，或上传 `.txt` 文件。
*   **销毁机制**：上传 3 天后自动永久删除。
*   **展示**：主页下方平铺展示最新的 50 条公开文档。

### 2.3 私密快递模式
*   **文件限制**：支持 `.zip` 压缩包上传（最大 50MB）。
*   **智能处理**：单文件小于 5MB 则原样保存。多个文件或大文件尝试压缩成 `.zip`。
*   **销毁策略**：5 次提取或 2h/6h/12h/24h 预设时长达成即销毁。
*   **提取码**：随机生成 6 位数字码。

### 2.4 基础设施韧性 (Hybrid Resilience)
*   **混合存储**：系统优先尝试使用 Firebase Firestore & Storage。
*   **自动兜底**：当云端服务由于权限或配置不可用时，系统会自动无缝切换至本地 JSON 存储模式 (`local_db.json`)，确保服务始终可用。

## 3. 技术栈
*   **前端**：React 18 + Vite + Tailwind CSS + Framer Motion + Lucide React。
*   **后端**：Node.js (Express) + Firebase Admin SDK + Multer + AdmZip。
*   **存储**：Firebase (Cloud) / Local JSON (Fallback)。

## 4. 部署方案
*   **多环境支持**：支持 Cloud Run (Docker)、标准 VPS、以及具有 Node 运行环境的 NAT 服务器。
*   **零配置启动**：系统默认开启本地兜底模式，即使没有任何云端配置，也能在只有命令行的开发者沙盒中一键启动。
*   **反向代理协同**：完美适配 Nginx 反向代理、Cloudflare Tunnel 或 frp 穿透方案。

