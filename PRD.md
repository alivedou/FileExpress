# 项目需求文档 (PRD)：文件快递柜 (FileExpress)

## 1. 项目概述
本项目是一个轻量级、极简风格的临时文件存储与共享平台。用户可以通过取件码或公开链接安全地传递临时文档。

**当前状态**：项目已进入成熟阶段，拥有完备的本地化管理工具 (FE CLI) 和确定的本地加密存储架构。

## 2. 核心功能

### 2.1 基础交互
*   **极简 UI**：采用简约设计语言，支持**暗色 (Dark)** 和 **亮色 (Light)** 模式切换。
*   **交互逻辑**：主页采用“存放/提取”切换模式。布局紧凑，首屏可见广场内容。
*   **多语言支持**：支持**中文**和**英文**。

### 2.2 公开广场 (Public Square)
*   **格式**：仅限 `.txt`。支持在线编辑标题和内容直接生成，或上传 `.txt` 文件。
*   **销毁机制**：上传 3 天后自动永久删除。
*   **展示**：主页下方平铺展示最新的 50 条公开文档。

### 2.3 私密快递模式
*   **文件限制**：支持多种常用格式及 `.zip` 压缩包上传（最大 50MB）。
*   **安全加固**：所有上传到私密柜的文件均经过 AES-256-GCM 物理加密存储。
*   **销毁策略**：5 次提取或 2h/6h/12h/24h 预设时长达成即销毁。
*   **提取码**：随机生成 6 位数字码。

### 2.4 稳定性与管理 (Management)
*   **FE CLI**：内置交互式命令行管理工具，支持一键配置权限、初始化数据库、查看运行状态。
*   **容量熔断**：具备存储配额自动监测机制。当存储接近上限时，自动清理过期文件，若清理后仍超限则禁止上传，防止撑爆磁盘。
*   **反向代理协同**：完美适配 Nginx 反向代理、Cloudflare Tunnel 或 frp 穿透方案。

## 3. 技术栈
*   **前端**：React 18 + Vite + Tailwind CSS + Framer Motion + Lucide React。
*   **后端**：Node.js (Express) + Multer + AdmZip + Crypto (内建加密)。
*   **存储**：本地加密文件系统 + JSON 元数据存储。

## 4. 部署方案
*   **VPS / 物理机**：推荐部署方式。通过 `fe.sh` 进行傻瓜式配置。
*   **Docker / Cloud Run**：支持容器化部署。
*   **Cloudflare 兼容性**：当前版本为 Node.js 优化版，预留了迁移至 Cloudflare R2/D1 的技术架构思路。

