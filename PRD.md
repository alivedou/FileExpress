# PRD - 文件快递柜 (File Express)

## 1. 产品定义
**文件快递柜** (V2.5.0) 是一款基于 Web 的、安全的、临时的文件中转应用。

## 2. 核心功能
### 2.1 存放 (Deposit)
- **私密模式**：
  - 用户上传 1-10 个文件，新增支持：图片、文本、办公文档(`.doc`, `.xls`, `.ppt`, `.pdf`)及压缩包(`.zip`, `.rar`)。
  - 服务器自动对多文件进行内存级 ZIP 打包。
  - 采用 **AES-256-GCM** 加密，密钥不存库。
  - 生成 6 位取件码，支持扫码提取。
  - **用户自定义规则 (V2.4.0 更新)**：用户可在前端通过精准的数值输入框（支持 +/- 微调），自由设置私密文件自毁前的**保存时长（默认 2h）**与**最大提取次数（默认 5次）**。这两个数值的上限由系统环境配置控制 (MAX_STORAGE_HOURS 与 MAX_DOWNLOADS)，确保了资源不被单用户恶意占用消耗。UI 经过重新紧凑设计，确保移动端小屏幕一屏可见关键操作。
- **公开模式**：
  - 仅限 .txt 文件上传或在线粘贴文本片段，并可自动生成对应的 .txt 文档。
  - **最新调整**：同名公共文件强制实施“新文件覆盖老文件”逻辑（例如没有指定标题时默认生成的 'untitled.txt' 会相互覆盖，达到一定程度清理作用）。
  - 有效期固定为 3 天。

### 2.2 提取 (Extract)
- **流式下载 (V2.1.0 优化)**：
  - 弃用 Base64 前端构件法，改用 `GET` 接口流式直连。
  - 解决手机浏览器对大文件/Blob 下载的兼容性问题。
- **用户确认逻辑**：
  - 扫码后自动填充取件码，但由用户手动点击“验证提取”以增强操作感和体验性。

## 3. 技术与稳定性
- **熔断与清理**：
  - 每当服务器启动或存储接近上限时，自动执行 `GC`（清理过期文件）。
  - **Multer 拦截**：增加中间件层，防止超大包绕过前端限制耗尽内存。
- **响应式优化 (移动端优先)**：
  - 更新结果页 UI 布局（成功提示字号调小、提取码留白紧凑化、二维码缩小）。
  - 实现手机端无需滚动页面即可一屏完整浏览取件信息及扫码区域。
  - **最新调整**：将成功提示文本与二维码采用并排（左右）混合布局，压缩了头部高度并美化了视觉呈现，使界面能在同一屏内展示更有效的信息。
- **下载计数与销毁逻辑修复**：
  - 修复最后一次提取失败的 Bug。将提取次数的递增和最大次数到达后的自动物理销毁机制从 POST 提取验证路由转移到 GET 下载主路由中。确保真实发送文件流后再销毁文件。

## 4. 界面设计 (Design Logic)
- **视觉风格**：Minimalist Tech / Cyber Style。
- **交互**：分段控件、滑块及 Framer Motion 平滑动画。

## 5. 项目工程化与部署 (V2.4.0 最新完善)
- **管理终端 (`fe.sh` CLI)**：
  - 首创基于 bash 的交互配置面板，支持一件配置环境、限额、最大提取次数及网络端口设置。
  - **优雅进程管理**：拦截 `Ctrl+C` 事件优雅地退出全台运行测试环境。同时新增 **后台稳定挂载运行** (`nohup`) 模式和一键服务停止状态机，不污染其它开发工作。
  - **动态可控参数（端口与生命周期）**：新增 `APP_PORT`（按需自定义运行端口容灾避让），联动前端 UI 动态计算生成的 `MAX_STORAGE_HOURS` 变量（供管理员一键调整平台支持的私密快递生命周期最大跨度）。
  - **管理终端高级特性集成**：更新新增的独立 `高级选项` 子菜单，整合了**全局 `fe` 快捷唤醒指令**安装（以便在任意终端随处管理），并封装了真正的**一键全量干净卸载/自毁**程序（能够联动阻断后台挂载、卸载全局快速指令、并自行清理执行路径全系数据），解决了单纯由于 `rm` 导致的残留服务隐患。
  - **跨平台与多形式部署支持**：兼容 Local/WSL 局域网开箱即用体验、Linux VPS 后台守护进程模式，并能够完美契合 Cloudflare Tunnels (Zero Trust) 实现无公网 IP 和免端口放行的高级内网穿透与外网映射。
- **公共接口支持**：
  - 针对公共文件提供 API 调用接口（兼容原生的 `POST /api/upload/public` 发送 `application/json` 数据或使用 `/api/batch-upload` 发送文件形式）。无需任何修改，允许极客和前端随时通过 cURL 或 Python 进行自动化跨端文件投递测试。
  - 对于无标题的公共提交统一回退到 `untitled.txt` 并依靠覆盖逻辑限制公共空间污染。

## 6. 移动端与扫码优化 (V2.4.1 最新完善)
- **极速识别二维码**：
  - **高对比高对比度配置**：生成的二维码去除了原有的半透明背景，改为纯白面框不透明底色（`#ffffff`），像素颜色更改为极高识别速度的深蓝黑色（`#0f172a`），带 2 个标准安全留白格子。
  - **物理渲染框扩大**：前端渲染尺寸提高至 `w-20`（移动端）和 `w-24`（智能屏幕），大幅缩短扫码摄像头对焦拉风箱现象，实现百毫秒内瞬时对焦提取。
- **移动端多重下载保护与并发防去重机制**：
  - **并发防刷保护**：在 `GET /api/download/:code` 自毁接口中内置了时间周期滑动窗口算法。以客户端 IP+物理记录ID 作为键，对 6 秒以内的多次冗余请求实行强力去重（只计 1 次实际消费）。
  - **解决机制痛点**：杜绝了如部分 iOS Safari、微信/UC浏览器、搜狗浏览器在渲染提取页面时，因后台静默预取、发起分片 Range 字节请求、爬虫校验、二次扫码，造成的下载额度瞬间被吞没导致提前物理自毁销毁文件的故障问题。
- **公网连接向导优化**：
  - **外网多维环境侦测**：CLI 脚本 (`fe.sh`) 基于轻量异步超时判定，除了输出本地内网/虚拟机 IP，还将自动通过高可用节点提取服务器的物理外部公网展示出来（外部公网地址格式化为 `http://公网IP:端口`），彻底解决了非专业对口用户在轻量云服务器/VPS 环境下，因后台只输出 `主私网 IP / localhost` 导致的连接懵圈感与迷茫。

## 7. 现代 Node.js 22/24 支持与技术生态友好度 (V2.5.2 收尾完善)
- **多级路径智能适配 (Cloud Native)**：V2.5.2 进一步优化了静态资源寻址逻辑，采用优先级扫描机制适配 `PM2/Docker` 等各种生产环境启动方式，配合 `base: './'` 实现零配置下的样式 100% 正常加载。
- **UI 交互纯净化**：移除了指向不明的提示信息，主打“开箱即用”的极致极简体验。
- **构建路径标准化**：配置 `base: './'` 以支持标准的公网根路径部署。
- **Node.js 22 / 24 黄金级兼容保障**：
  - 加密模块 (`crypto` - AES-256-GCM) 采用标准核心二进制库，无第三方 C++ 二进制扩展依赖（零 native bindings），能 100% 畅通地运行于 Node.js 22 LTS 乃至最新的 24 Current 运行状态而无惧环境报错。
  - 热开发引擎 `tsx` 及构建工具 `Vite` 完全适配最新 V8 编译器，在 Node.js 22/24 下可享受更敏捷的冷启动响应、更卓越的堆内存回收机制和超低的文件 I/O 延迟。
  - 最新版本支持管理员在 Node 22+ 下开启 `--experimental-strip-types` 标志以直接剥离类型运行，实现零构建直接部署运行（Zero-Build Deployment）。
- **代码全面收尾度量与极致简洁性**：
  - **类型系统合规**：所有 TS 类型定义及约束机制（如 `tsconfig.json`）均已获得通过，核心数据通过 `tsc --noEmit` 审查，消灭隐式 any 类型，最大化利用编译器类型防错。
  - **无死角逻辑确认**：文件存、取、ZIP 物理自动构件打包、AES 加密分片保护、下载量精确定位防去重等业务路由皆通过内部深度诊断，杜绝虚假或非授权占用。
  - **交互体验合规**：主页分段式卡片布局（存放/提取）、各处数字微调框、公开广场、语言状态机等元素经多端口交互模拟，全站按钮和菜单操作保持完全可用。
- **技术狂热者 (Tech Enthusiasts) 极客特质**：
  - 支持 cURL 物理直传和 Python 批量自动化派件接口。
  - 提供了优雅的 `fe.sh` 进程挂载与整套高级自毁自洁脚本方案，对本地二次开发和私有云端 DIY 极度友好。
